extends layout
    
block content
    div User logged in is #{user.local.email}
    div You can always
        a(href="/logout")  Logout

    img(src="avatars/#{user.local.avatar}")

    div
        form(method="POST" enctype="multipart/form-data")
            input(type="file" name="avatar")
            input(type="submit" value="submit")